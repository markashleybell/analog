<Window x:Class="analog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:analog"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="76" MinHeight="76"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="textBox" Margin="0" Grid.Row="0" Grid.Column="0" TextWrapping="Wrap" AcceptsReturn="True" Text="select * from entries"/>
        <Button x:Name="button" Content="Button" Grid.Row="0" Grid.Column="1" Margin="0" Click="button_Click"/>
        <DataGrid x:Name="dataGrid" Margin="0" Grid.Row="1" Grid.ColumnSpan="2" IsReadOnly="True" CanUserAddRows="False" ItemsSource="{Binding}"/>

    </Grid>
</Window>
